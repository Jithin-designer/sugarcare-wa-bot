# Secrets — never commit
.env
.env.save
.env.*
!.env.example

# Local live-deploy notes — contains the real phone number, Phone Number ID,
# and WABA ID. Business-confidential; keep on disk but never commit.
LIVE-DEPLOY.md

# Runtime data (SQLite db + mock outbox). `data/` already covers everything
# below; the explicit db entries are listed too for belt-and-suspenders safety
# (the WAL/SHM sidecar files created by journal_mode=WAL must never be committed).
data/
data/bot.db
data/bot.db-wal
data/bot.db-shm

# Dependencies
node_modules/

# Logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
